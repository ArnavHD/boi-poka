import React, { use } from "react";

const Book = ({ book }) => {
  //   const data = use(bookPromise);
  //   console.log(data);

  console.log(book)
  return (
    <div>
      <h1>Just book</h1>
    </div>
  );
};

export default Book;
